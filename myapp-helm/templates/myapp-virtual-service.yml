apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: myapp-virtualservice
  namespace: jhipster-project
spec:
  hosts:
    - myapp
  http:
    - route:
        - destination:
            host: myapp
            subset: "v1"
          weight: 100
      retries:
        attempts: 3
        perTryTimeout: 2s
